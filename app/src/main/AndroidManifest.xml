<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahidanehal21.letstalk3"
    android:installLocation="internalOnly">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Authentication" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CreateNewAccountActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".AllContacts"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".chatwindo" />
        <activity android:name=".email_login" />
        <activity android:name=".send" />
        <activity android:name=".email_registration" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".profile_acti" />
        <activity android:name=".Status_change_activity"></activity>
    </application>

</manifest>